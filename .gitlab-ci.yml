image: python:2.7

build_wheel:
  before_script:
    - pip install --upgrade bumpversion
    - git config --global user.email "cedric.messiant@gmail.com"
    - git config --global user.name "Cédric Messiant"
    - git remote set-url --add github git@github.com:ecreall/flask-example.git
    - git fetch github
  script:
    - git checkout master
    - bumpversion minor
    - git status
    - git diff
    - git push github HEAD:add_build_job